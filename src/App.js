import React from 'react';
import {
  View,
  Text,
} from 'react-native';
import MainStackNavigator from './navigation/MainStackNavigator'

const App = props => {
    return (
      <MainStackNavigator />
    );
}

export default App;